import EditContactCell from 'src/components/Modules/CRM/Contact/EditContactCell'

const EditContactPage = ({ id }: { id: string }) => {
  return <EditContactCell id={id} />
}

export default EditContactPage
