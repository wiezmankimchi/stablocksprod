import EditExpenseCell from 'src/components/Modules/Finance/Expense/EditExpenseCell'

const EditExpensePage = ({ id }: { id: string }) => {
  return <EditExpenseCell id={id} />
}

export default EditExpensePage
