{
  "private": true,
  "workspaces": {
    "packages": [
      "api",
      "web",
      "packages/*"
    ]
  },
  "devDependencies": {
    "@redwoodjs/core": "^0.42.0"
  },
  "scripts": {
    "postgres:up": "docker-compose up -d",
    "postgres:down": "docker-compose down",
    "postgres-test:up": "docker-compose -f docker-compose-test.yml up -d",
    "postgres-test:down": "docker-compose -f docker-compose-test.yml down",
    "dev": "yarn postgres:up && yarn rw dev",
    "test": "yarn postgres-test:down && yarn postgres-test:up && yarn rw test"
  },
  "eslintConfig": {
    "extends": "@redwoodjs/eslint-config",
    "root": true
  },
  "engines": {
    "node": ">=14.17 <=16.x",
    "yarn": ">=1.15 <2"
  },
  "prisma": {
    "seed": "yarn rw exec seed"
  }
}
